#include "monty.h"

/**
 * op_nothing - does nothing
 * @stack: The pointer to the top of the stack
 * @line_number: line number
 **/
void op_nothing(stack_t **queues_stack, unsigned int line_number)
{
	(void)queues_stack;
	(void)line_number;
}
